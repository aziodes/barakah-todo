node_modules/
.next/
out/
build/

# Local env files — never commit real keys
.env
.env.local
.env*.local

# Firebase service-account keys. FIREBASE_SERVICE_ACCOUNT belongs in Vercel
# env vars, not in the repo.
serviceAccount.json
*-firebase-adminsdk-*.json
.firebase/
firebase-debug.log

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
.env*
# ...but the placeholder template is meant to be committed. Without this the
# broad .env* above would silently drop it if it were ever recreated.
!.env.local.example
